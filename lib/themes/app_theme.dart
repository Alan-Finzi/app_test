import 'package:flutter/material.dart';

class AppTheme{
    // Background Colors
    static Color backgroundPrimaryDark = Color.fromRGBO(45, 42, 38, 1);
    static Color backgroundEmpty = Color.fromRGBO(250, 250, 250, 1);

    // Accents
    static Color accentDarkGreen = Color.fromRGBO(0, 120, 83, 1);
    static Color accentLightGreen = Color.fromRGBO(226,244,62,255);

    // Customs
    static Color lightGrey = Color.fromRGBO(220, 220, 220, 1);

    // Optionals
    static Color success = Color.fromRGBO(147, 213, 0, 1);

}